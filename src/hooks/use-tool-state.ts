/**
 * Dedicated hook for selecting tool execution state from the thread store.
 * Re-exported from use-thread-selectors for convenience — individual tool blocks
 * import from this module so the dependency is explicit.
 */
export { useToolState } from "./use-thread-selectors";
