The task agents should know the context that this is a claude code desktop UI that
should be very keyboard friendly. They should understand how the claude code functionality works and output a plan to implement the feature.\\

These task agents should be aware of the testing strategy and anthropic SDK types. Many will be rendering displays for the anthropic tool calls ouput through the agent SDK.

DO NOT read all the context from the sub agents, just trust them to do planning and provide them with the high level goals and their particular feature.
